# Dependencies
node_modules/
vendor/

# Committed build artifacts (generated by `npm run build` — never hand-format)
build/

# Lockfiles
package-lock.json
composer.lock

# PHP is formatted to WordPress standards via PHPCS, not Prettier
*.php

# Block markup — its canonical serialization is defined by each block's save(),
# not by Prettier. Reformatting the style/void-element syntax diverges from what
# the editor emits and risks invalid-block warnings, so leave it untouched.
parts/
templates/
patterns/
